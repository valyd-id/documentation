# Face match (moved)

> 📄 **This page is retired.** Face match is not offered as a direct public API — it runs as a
> check inside a [Reusable Verification](/verifications) workflow, matching a selfie against the
> connected account's stored face vector. See the [checks reference](/verifications/types).
