# Age check

> 📄 **This page moved.** Age verification is documented in one canonical place:
> **[Age verification](/verifications/standalone/age-verification)**. This stub stays so old links
> keep working.

Running an age check for a signed-in user is the **same call** as the one-off check — you just add
the user's `valyd_access_token`, so the age band (`is_18_plus`, …) saves to their account as a
proof. See [Age verification](/verifications/standalone/age-verification).
